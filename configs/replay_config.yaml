model: gpt-4o
base_url: null
temperature: 0.0
max_retries: 3

prompts:
  system: system_prompt.md
  step: step_prompt.md

chunker:
  max_events: 15
  max_event_types: 2
  max_hours: 2.0
  stop_at:
    event_type: SERVICE
    description: "Service: SURG"
